@workspace I have saved all the secrets successfully. We have limited AI quota, so execute these 4 major upgrades in a single, flawless run without exploratory steps:

1. AURA "Real Tamil" Persona & Greeting:

When AURA is awakened (wake word or mic click), update the speech synthesis greeting to exactly: "Vanakkam, naanthaan Aura, sollunga!" using a clear, natural voice.

Update the Pollinations system prompt to force AURA to speak in natural, colloquial Tamil/Tanglish when responding to general queries. It must sound like a real, conversational human assistant.

2. Advanced Google-Style Listening Glow:

Replace the basic neon cyan outline with a highly advanced, colorful, animated border overlay that mimics Google Assistant.

Implementation: Create a fixed full-screen pointer-events-none overlay with a z-index: 9999. Use a CSS @keyframes animation with a moving conic-gradient or animated multi-color box-shadow (combining Google-like colors: #4285F4, #EA4335, #FBBC05, #34A853, or fitting cyberpunk neon equivalents). It should smoothly pulse and rotate ONLY when AURA is actively listening.

3. Zero-Latency Execution & Preloading (Instant Speed):

Local Command Interception: In AuraService.ts (or the command parser), intercept commands BEFORE they hit the Pollinations API. If the transcript matches a regex like /open\s+(.*)/i or /(\w+)\s+open/i (e.g., "open browser", "browser open pannu"), immediately trigger the openApp action from the Store and abort the API call.

App Preloading: Implement a background preloading mechanism. In the main Desktop.tsx useEffect, dynamically import and cache the heavy app components (like Notepad, Browser, SOC) so they render instantly upon opening.

4. Advanced Chess Game Notification:

Overhaul the UI for the Chess Game notifications/toasts.

Upgrade it to match the premium OS theme: Use strict dark glassmorphism (bg-black/60 backdrop-blur-lg), a subtle glowing border, a sleek icon (like a neon knight ♞), and smooth slide-in/slide-out framer-motion animations.

Execute these UI, state, and logic changes perfectly.